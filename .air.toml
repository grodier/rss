root = "."
tmp_dir = ".tmp"

[build]
cmd = "go build -o ./.tmp/main ./cmd/api/"
bin = "./.tmp/main -port 8080"
include_ext = ["go"]
exclude_dir = [".tmp", "vendor", "node_modules"]
delay = 1000

[log]
time = true

[misc]
clean_on_exit = true
